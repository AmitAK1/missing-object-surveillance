# Email Alert Configuration (Optional)
# Copy this file to .env and fill in your credentials

# Gmail Settings
EMAIL_SENDER=your-email@gmail.com
EMAIL_PASSWORD=your-gmail-app-password-here
EMAIL_RECIPIENTS=recipient@gmail.com

# SMTP Server Settings (for Gmail)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Instructions:
# 1. Go to Google Account → Security → 2-Step Verification
# 2. Generate an "App Password" for Mail
# 3. Use that 16-character password (no spaces) as EMAIL_PASSWORD
# 4. Save this file as .env (remove .example extension)
#
# Note: Email alerts will work without this file, 
# but no emails will be sent
