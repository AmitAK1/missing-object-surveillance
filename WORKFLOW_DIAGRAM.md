# Visual Workflow - Missing Object Surveillance System

## Program Flow Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         START PROGRAM                               │
│                      python main.py                                 │
└───────────────────────────────┬─────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    STAGE 1: LIVE PREVIEW                            │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  📹 LIVE VIDEO FEED                                         │   │
│  │  ┌─────────────────────────────────────────────┐           │   │
│  │  │  [Camera shows real-time video]             │           │   │
│  │  │                                              │           │   │
│  │  │  Press 'c' to capture frame                 │           │   │
│  │  │  Press 'q' to quit                          │           │   │
│  │  └─────────────────────────────────────────────┘           │   │
│  │                                                             │   │
│  │  Keys: [c] Capture  [r] Refresh  [q] Quit ◄──────────────┐ │   │
│  └─────────────────────────────────────────────────────────┬─┘ │   │
└────────────────────────────────────────────────────────────┼───┼───┘
                                                             │   │
                                      Press 'c' ─────────────┘   │
                                      Press 'q' ──────────────────┼───► EXIT
                                                                  │
                                ┌─────────────────────────────────┘
                                │ Frame Captured!
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   STAGE 2: MODE SELECTION                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Press 's' for SINGLE ROI mode                              │   │
│  │  Press 'm' for MULTIPLE ROI mode                            │   │
│  └─────────────────────────────────┬───────────────────────────┘   │
└────────────────────────────────────┼───────────────────────────────┘
                                     │
                 ┌───────────────────┴────────────────────┐
                 │                                        │
         Press 's'                                  Press 'm'
                 │                                        │
                 ▼                                        ▼
┌─────────────────────────────────┐    ┌──────────────────────────────────┐
│  STAGE 3a: SINGLE ROI           │    │  STAGE 3b: MULTIPLE ROI          │
│  ┌───────────────────────────┐  │    │  ┌────────────────────────────┐  │
│  │ Draw ONE box:             │  │    │  │ Draw box #1 → ENTER        │  │
│  │ ┌───────────────────────┐ │  │    │  │ Press 'a' to add more      │  │
│  │ │ [Frozen Frame]        │ │  │    │  │ Draw box #2 → ENTER        │  │
│  │ │                       │ │  │    │  │ Press 'a' or other key     │  │
│  │ │   ┌─────────┐         │ │  │    │  │ ...                        │  │
│  │ │   │  📱     │         │ │  │    │  │ ┌─────┐  ┌─────┐  ┌─────┐ │  │
│  │ │   │  ROI-1  │         │ │  │    │  │ │ 🔑  │  │ 💼  │  │ 📱  │ │  │
│  │ │   └─────────┘         │ │  │    │  │ │ROI-1│  │ROI-2│  │ROI-3│ │  │
│  │ │                       │ │  │    │  │ └─────┘  └─────┘  └─────┘ │  │
│  │ └───────────────────────┘ │  │    │  │                            │  │
│  │ ENTER to confirm          │  │    │  │ Keys: ENTER, 'a', 'c'      │  │
│  │ 'c' to cancel & redraw    │  │    │  └────────────────────────────┘  │
│  └───────────────────────────┘  │    └──────────────────────────────────┘
└─────────────────┬───────────────┘                    │
                  └──────────────────┬─────────────────┘
                                     │
                                     ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   STAGE 4: SURVEILLANCE                             │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  🎥 LIVE MONITORING                                         │   │
│  │  ┌─────────────────────────────────────────────┐           │   │
│  │  │  STATUS: All Secured / ALERT                │           │   │
│  │  │                                              │           │   │
│  │  │  ┌──────┐         ┌──────┐         ┌──────┐│           │   │
│  │  │  │  🔑 │  🟢     │  💼 │  🟢     │  📱 │🟢│           │   │
│  │  │  │ ROI-1│         │ ROI-2│         │ ROI-3││           │   │
│  │  │  └──────┘         └──────┘         └──────┘│           │   │
│  │  │                                              │           │   │
│  │  │  Press 'r' to re-select | 'q' to quit       │           │   │
│  │  └─────────────────────────────────────────────┘           │   │
│  │                                                             │   │
│  │  🟢 Green = Secured  🟡 Yellow = Init  🔴 Red = ALERT     │   │
│  └─────────────────────────────────────────────────────────┬─┘   │
└────────────────────────────────────────────────────────────┼───┬─┘
                                                             │   │
                                      Press 'r' ─────────────┘   │
                                      Press 'q' ──────────────────┼───► EXIT
                                                                  │
                                ┌─────────────────────────────────┘
                                │ (goes back to STAGE 1)
                                │
                                ▼
                           LIVE PREVIEW
                           (cycle repeats)
```

---

## State Transition Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                     Object Detection States                 │
└─────────────────────────────────────────────────────────────┘

   ┌─────────────────┐
   │  INITIALIZING   │ ◄──── Program starts
   │   (Yellow)      │       or ROI re-selected
   └────────┬────────┘
            │
            │ Object detected in ROI
            │
            ▼
   ┌─────────────────┐
   │    SECURED      │ ◄──┐
   │    (Green)      │    │
   └────────┬────────┘    │
            │              │
            │ Object       │ Object
            │ missing      │ reappears
            │              │
            ▼              │
   ┌─────────────────┐    │
   │ Missing Counter │    │
   │  Incrementing   │    │
   │   (counting)    │    │
   └────────┬────────┘    │
            │              │
            │ Counter >    │
            │ THRESHOLD    │
            │              │
            ▼              │
   ┌─────────────────┐    │
   │     ALERT!      │    │
   │     (Red)       │    │
   │  Save snapshot  │ ───┘
   └─────────────────┘
```

---

## Multi-ROI Monitoring Logic

```
┌──────────────────────────────────────────────────────────┐
│           How Multiple ROIs Are Monitored                │
└──────────────────────────────────────────────────────────┘

Each Frame:
  ┌──────────────────────────────────────────────┐
  │  FOR EACH ROI in roi_targets:                │
  │                                               │
  │    1. Extract ROI region from frame          │
  │       roi_frame = frame[y1:y2, x1:x2]        │
  │                                               │
  │    2. Run YOLO on this ROI only              │
  │       results = model(roi_frame)             │
  │                                               │
  │    3. Check if target object present         │
  │       if target in detected_objects:         │
  │           object_present = True              │
  │                                               │
  │    4. Update this ROI's state manager        │
  │       state = state_manager.update(...)      │
  │                                               │
  │    5. Draw colored box based on state        │
  │       Green/Yellow/Red                       │
  │                                               │
  │  END FOR                                      │
  │                                               │
  │  6. Check if ANY ROI is in ALERT state       │
  │     if any_alert: save snapshot              │
  │                                               │
  └──────────────────────────────────────────────┘

Independence:
  - Each ROI has its own StateManager
  - Each ROI has its own missing_counter
  - Each ROI's state is independent
  - Overall alert triggers if ANY ROI alerts
```

---

## Key Press Handling Flow

```
┌────────────────────────────────────────────────────────┐
│                 Keyboard Event Handler                 │
└────────────────────────────────────────────────────────┘

LIVE PREVIEW MODE:
  ┌─────────────────────────────────────────┐
  │  key = cv2.waitKey(1) & 0xFF            │
  │                                          │
  │  if key == ord('c'):                    │
  │      → Capture frame                    │
  │      → Go to Mode Selection             │
  │                                          │
  │  elif key == ord('r'):                  │
  │      → Continue live preview            │
  │                                          │
  │  elif key == ord('q'):                  │
  │      → Cleanup and exit                 │
  └─────────────────────────────────────────┘

SURVEILLANCE MODE:
  ┌─────────────────────────────────────────┐
  │  key = cv2.waitKey(1) & 0xFF            │
  │                                          │
  │  if key == ord('q'):                    │
  │      → Break loop                       │
  │      → Cleanup and exit                 │
  │                                          │
  │  elif key == ord('r'):                  │
  │      → Destroy windows                  │
  │      → Go back to Live Preview          │
  │      → Re-select ROIs                   │
  │      → Resume surveillance              │
  └─────────────────────────────────────────┘
```

---

## Memory & Performance

```
┌────────────────────────────────────────────────────────┐
│              Resource Usage Per ROI                    │
└────────────────────────────────────────────────────────┘

Single ROI:
  Memory:  ~50MB (YOLO model) + ~10MB (frame buffers)
  CPU:     20-40% (depends on ROI size)
  FPS:     20-30

Multiple ROIs (3 ROIs):
  Memory:  ~50MB (YOLO shared) + ~15MB (frame buffers)
  CPU:     40-60% (3x inference)
  FPS:     15-25

Performance Tips:
  ✓ Smaller ROI = Faster processing
  ✓ Fewer ROIs = Higher FPS
  ✓ yolov8n.pt = Fastest model
  ✓ Share same model instance across ROIs
```

---

## Error Handling Flow

```
┌────────────────────────────────────────────────────────┐
│                  Error Scenarios                       │
└────────────────────────────────────────────────────────┘

Camera not accessible:
  cap.isOpened() == False
    → Print error
    → Return from main()
    → EXIT

Frame read fails:
  success, frame = cap.read()
  if not success:
    → Print "Video feed ended"
    → Break loop
    → Cleanup
    → EXIT

ROI selection cancelled:
  roi == (0, 0, 0, 0)
    → Print "ROI cancelled"
    → Continue loop
    → Ask to draw again

No objects in multiple ROI mode:
  if not rois_list:
    → Print "No ROIs selected"
    → Continue with previous setup
    → Resume surveillance

Window cleanup fails:
  → Force with cv2.waitKey(1) x2
  → Use try-except blocks
  → Ensure cleanup in finally
```

---

*Use this diagram as a visual reference while using the system!*
